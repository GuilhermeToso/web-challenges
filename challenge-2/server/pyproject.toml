[tool.poetry]
name = "server"
version = "0.1.0"
description = ""
authors = ["Guilherme M. Toso <gui.m.toso@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = {extras = ["stanndard"], version = "^0.115.6"}
uvicorn = "^0.33.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
